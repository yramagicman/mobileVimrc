#!/bin/zsh
os=$(uname)
now=$(date +"%m-%d-%Y")
echo $now
cd ~/.mutt
pwd
sleep 3s
gzip Sent
tar -czf cache.tar.gz cache
mv Sent.gz "sent$now$os.gz"
mv cache.tar.gz "cache$now$os.tar.gz"
rm -rf cache
touch Sent
git add --all
git commit -m "backup sent emails $now"
git push
